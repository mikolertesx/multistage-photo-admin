{
  "name": "multistage-photo-admin",
  "private": true,
  "description": "React monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "preview": "pnpm --filter web preview"
  },
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.4"
}
